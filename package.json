{
  "name": "ignite-ui",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  }
}
